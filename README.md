# Instructions 
## Prerequisite 
+ vim-9.1 should be compiled with python support. 
+ nodejs/npm/yarn 
+ pyright
+ autopep8
+ black

## Install
+ download the repo to `~/.vim/`
+ execute `Plug Install` in vim
+ execute `CocInstall Coc-python`, `CocInstall Coc-pyright`

